# Example TLS configuration metadata.
# This intentionally documents the classical certificate mechanism
# so the repository has an additional crypto inventory signal.

TLS_VERSION = "TLSv1.3"
CERTIFICATE_ALGORITHM = "ECDSA-P256"
CERTIFICATE_CURVE = "secp256r1"